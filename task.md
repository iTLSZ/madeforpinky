# Made For Pinky (solo local)

## Estado

- Proyecto configurado para uso 100% local, sin API ni base de datos.
- La configuración (nombre, fotos, música, etc.) se define en `jscp/settings.js` (función `initializeDefaultSettings`).
